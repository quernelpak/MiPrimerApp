<h1>Linea creation</h1>

<form action="{{ path('linea_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('linea') }}">
            Back to the list
        </a>
    </li>
</ul>
